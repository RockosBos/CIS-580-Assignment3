Instructions for running Assignment 3 Project

1. Run "pip install -r ./requirements.txt"
	-This will install all the required packages for the project into your environment or virtual environment.

2. Run "Python app.py" with the required packages installed.
	- Console output will notify you when the script has been completed.

3. Check bug_localization_ranks.csv for final results!
